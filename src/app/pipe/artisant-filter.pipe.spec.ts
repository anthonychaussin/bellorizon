import {ArtisantFilterPipe} from './artisant-filter.pipe';

describe('ArtisantFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ArtisantFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
